# Copy to `.env` (gitignored) only if anonymous access starts throttling.
# Every source in this repo works keyless at the volumes we pull. All keys are free.

# data.cdc.gov Socrata app token — raises the anonymous rate limit on the
# CDC Alzheimer's & Healthy Aging bulk pull. https://evergreen.data.socrata.com/signup
SOCRATA_APP_TOKEN=

# BLS Public API v2 key — only needed for the API fallback path if the
# download.bls.gov flat-file server is unavailable. https://data.bls.gov/registrationEngine/
BLS_API_KEY=

# api.census.gov key — unnecessary at our request volume (a handful of ACS calls).
# https://api.census.gov/data/key_signup.html
CENSUS_API_KEY=
